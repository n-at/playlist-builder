# playlist-builder

```bash
$ ./app -help
Usage of ./app:
  -dest string
        Destination playlist path, defaults to source file name without extension
  -dest-playlist string
        Destination m3u / m3u8 playlist file, defaults to source file name
  -src string
        Source m3u / m3u8 playlist file
```
